{
  "skill": "b200-mbarrier-protocol-auditor",
  "version": "0.1.0",
  "source_book": "Modern GPU Programming For MLSys (MLC Community) + NVIDIA Blackwell Tuning/Compatibility Guides",
  "darwin_compatible": true,
  "test_cases": [
    {
      "id": "should-trigger-01",
      "type": "should_trigger",
      "prompt": "My PIPE_DEPTH=2 kernel starts deadlocking on the second round; check the phase.",
      "expected_behavior": "Should activate and hand-compute the stage/phase sequence.",
      "notes": "Used to validate activation boundaries and execution method; does not mean it has been run on B200 hardware."
    },
    {
      "id": "should-trigger-02",
      "type": "should_trigger",
      "prompt": "The TMA has completed but the consumer still waits forever — where could expect_tx be wrong?",
      "expected_behavior": "Should activate and audit the arrival count, byte count, and barrier object.",
      "notes": "Used to validate activation boundaries and execution method; does not mean it has been run on B200 hardware."
    },
    {
      "id": "should-trigger-03",
      "type": "should_trigger",
      "prompt": "After tcgen05.mma is issued, the epilogue occasionally reads incomplete results.",
      "expected_behavior": "Should activate and check commit.mbarrier and the wait.",
      "notes": "Used to validate activation boundaries and execution method; does not mean it has been run on B200 hardware."
    },
    {
      "id": "should-not-trigger-01",
      "type": "should_not_trigger",
      "prompt": "The result is consistently transposed every time.",
      "expected_behavior": "Should not be invoked first; this looks more like a layout contract error.",
      "notes": "Used to validate activation boundaries and execution method; does not mean it has been run on B200 hardware."
    },
    {
      "id": "should-not-trigger-02",
      "type": "should_not_trigger",
      "prompt": "The linker cannot find libcudart.",
      "expected_behavior": "Should not activate; a build problem.",
      "notes": "Used to validate activation boundaries and execution method; does not mean it has been run on B200 hardware."
    },
    {
      "id": "edge-case-01",
      "type": "edge_case",
      "prompt": "The kernel does not deadlock but gives numerically wrong results with 1% probability.",
      "expected_behavior": "Should be invoked, but layout/out-of-bounds must be investigated in parallel; do not pin it on the barrier alone.",
      "notes": "Used to validate activation boundaries and execution method; does not mean it has been run on B200 hardware."
    }
  ],
  "minimum_pass_rate": 0.8,
  "notes": "All should_not_trigger cases must pass; edge_case must state the invocation boundary. These tests are prompt/behavior stress tests, not hardware performance tests."
}
