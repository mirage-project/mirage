integrated-HEAD (c80ebd68) LATE-CONTEXT capture, msl=897 = 256-token synthetic prompt + 640 decode steps + 1, mbt=16, page_size=256, gate_padding_rows ON, attn_q_pass=2, no admission cap at bs1/8 (bs16 uncapped for this capture) -- opt/m3i7/stage/armL/pertask_by_bs.csv. SUPERSEDES the M3-I10 matched-geometry (msl=353) capture this file used before, for a structural reason and not just freshness: at msl=353 the exact admission replay has NO prefill-free regime wider than FIVE live requests at bs8 or bs16 (the first request retires at iteration 112, the last finishes prefilling at 128 / 256), so that geometry cannot express a bs8 or bs16 decode step at all -- and the committed armA capture in fact fell through parse_profile's `hi <= lo` guard into the last-eight-iterations fallback, recording regime (1 live, 0 prefill, 1 decode, 1 token) and tokens_per_step=1 for both. msl=897 yields a genuine full-width 8-live decode step at bs8 ([170,656), 486 iterations) and 12/16 at bs16 ([720,733)), and it samples the decode-context band the vLLM reference table was itself captured at (556-896) -- the argument M3-I10 already accepted for the attention row, applied here to every stage. Window alignment: parse_profile's window is centred on concurrency.py's own midpoint iteration per bs (opt/m3i7/scripts/window_plan.py, which prints the regime enumeration it derives that from), so the per-stage wall spans and the step_us they are normalised against come from the same iterations. Anchor-QC (per-step task-type counts vs the compiled task graph) run per bs before publication.
