PREP-REVIEW: PASS
1. note — include/mirage/persistent_kernel/tasks/blackwell/topk_softmax_sm100.cuh:202-370 — The <=16-row bit-exactness argument is sound for shipped Qwen3.5 configurations. `live_rows` is loop-invariant; hoisting it cannot change values. The gated and ungated 16-row instantiations are independently SASS-identical. The only block barrier remains after the uniform row loop, so neither padding-row gating nor partial tiles introduce divergent barrier execution.

2. note — include/mirage/persistent_kernel/tasks/blackwell/topk_sigmoid_sm100.cuh:176-193 — The old mask is undefined at `THREADS_PER_ROW==32`: lanes 16-31 execute shuffle intrinsics while absent from the supplied member mask. The replacement correctly uses the full mask for 32-thread rows. For 16-thread rows, tile bases are multiples of the even `ROWS_PER_CTA`; an odd global row count therefore places the final row in subgroup 0, and lines 190-191 select `0x0000ffff`. A row that is last in an earlier tile but not globally last retains the full mask because both subgroups exist. The shuffle width still isolates the two 16-lane logical groups.

3. note — include/mirage/persistent_kernel/tasks/blackwell/topk_softmax_sm100.cuh:118-135,208-370; include/mirage/persistent_kernel/tasks/blackwell/topk_sigmoid_sm100.cuh:103-118,176-379 — Above-cap tiling has no unintended cross-tile state. Phase-0 routing initialization occurs once before the loop; per-row input zeroing occurs inside it, exactly once for every covered row. Reduction values, selected groups, weights, and temporary arrays are recreated inside each trip. All threads execute the same loop trip count, inactive partial-tile threads merely skip the row body, and the sole post-processing barrier is reached uniformly after the loop. No divergent-barrier hang is present.

4. note — include/mirage/persistent_kernel/tasks/blackwell/topk_softmax_sm100.cuh:370-380; include/mirage/persistent_kernel/tasks/blackwell/topk_sigmoid_sm100.cuh:379-390 — The row loop does not structurally worsen the Phase-7 race: compaction still runs once, after one block-wide barrier, and no compaction overlaps a later row tile. At the shipped default, relevant router SASS is identical, so exposure is unchanged. Above the former cap, correctly processing additional rows can create more marks and make the existing race more observable, but that is not grounds to block this default-inert prep commit.

5. should-fix — demo/qwen3_5/accept/opt/m3i5b/prep.md:§4.P4 — P0-P3 adequately falsify the kernel change: they cover both router families, odd sigmoid rows, later partial tiles, live real-graph rows, default byte identity, both padding-gate arms, and active-mask consistency. Before changing any default above 16, however, M3-I5c should be fixed and validated, or the window must add a high-repetition active-expert compaction stress test across the proposed maximum row count. Three performance repetitions and single unit-test executions cannot exclude the acknowledged order-dependent Phase-7 clobber whose opportunity increases with additional routed rows.

6. note — demo/qwen3_5/accept/opt/m3i5b/prep.md:§3.2,§4.P0 — The lone non-identical `k_softmax<VPT=8,E=256,rows=1>` unit-test instantiation is appropriately not called byte-identical. Its opcode/dataflow evidence is sufficient for prep, and P0 supplies the required hardware correctness check; it does not affect a committed graph.

7. note — commit 7bb2a90 — No commit-level blocking defect is evident. Defaults remain at `mbt=16`, shipped <=cap paths have direct SASS-identity evidence, the intentional sigmoid change removes undefined behavior, and above-cap behavior is gated by the pre-registered GPU window. The commit is safe to push to the shared branch as default-inert prep.
